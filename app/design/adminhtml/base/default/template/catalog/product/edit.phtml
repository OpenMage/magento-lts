<?php
/**
 * @copyright  For copyright and license information, read the COPYING.txt file.
 * @link       /COPYING.txt
 * @license    Academic Free License (AFL 3.0)
 * @package     default_default
 */

/**
 * Template for Mage_Adminhtml_Block_Catalog_Product_Edit
 *
 * @var Mage_Adminhtml_Block_Catalog_Product_Edit $this
 */
?>
<div class="content-header">
    <h3 class="icon-head head-products"><?php echo $this->getHeader() ?></h3>
    <p class="content-buttons form-buttons"><?php echo $this->getBackButtonHtml() ?>
    <?php echo $this->getCancelButtonHtml() ?>
    <?php if($this->getProductId()): ?>
        <?php echo $this->getDeleteButtonHtml() ?>
        <?php if($this->getProductSetId() && $this->getIsConfigured()): ?>
            <?php echo $this->getDuplicateButtonHtml() ?>
        <?php endif ?>
    <?php endif ?>
    <?php if($this->getProductSetId() && $this->getIsConfigured()): ?>
        <?php echo $this->getSaveButtonHtml() ?>
        <?php echo $this->getSaveAndEditButtonHtml() ?>
    <?php endif ?>
    </p>
</div>
<form action="<?php echo $this->getSaveUrl() ?>" method="post" id="product_edit_form" enctype="multipart/form-data">
    <?php echo $this->getBlockHtml('formkey')?>
    <div style="display:none"></div>
</form>
<script type="text/javascript">
//<![CDATA[
    var productTemplateSyntax = /(^|.|\r|\n)({{(\w+)}})/;
    var productForm = new varienForm('product_edit_form', '<?php echo $this->getValidationUrl() ?>');
    productForm._processValidationResult = function(transport) {
        var response = JSON.parse(transport.responseText);
        if (response.error){
            if (response.attribute && document.getElementById(response.attribute)) {
                document.getElementById(response.attribute).setHasError(true, productForm);
                Validation.ajaxError(document.getElementById(response.attribute), response.message);
                document.getElementById(response.attribute).focus();
            }
            else if (document.getElementById('messages')) {
                document.getElementById('messages').innerHTML = '<ul class="messages"><li class="error-msg"><ul><li>' + response.message + '</li></ul></li></ul>';
            }
        }
        else{
            productForm._submit();
        }
    };
    function saveAndContinueEdit(urlTemplate) {
        var url = urlTemplate.replace(productTemplateSyntax, function(m, p1, p2, p3) { var obj = {tab_id:product_info_tabsJsTabs.activeTab.id}; return p1 + (obj[p3] !== undefined ? obj[p3] : ''); });
        productForm.submit(url);
    }
    function setSettings(urlTemplate, setElement, typeElement) {
        var obj = {attribute_set:document.getElementById(setElement).value,type:document.getElementById(typeElement).value};
        var url = urlTemplate.replace(productTemplateSyntax, function(m, p1, p2, p3) { return p1 + (obj[p3] !== undefined ? obj[p3] : ''); });
        setLocation(url);
    }

     function setSuperSettings(urlTemplate, attributesClass, validateField) {
        var attributesFields = document.querySelectorAll('.' + attributesClass);
        var attributes = Array.from(attributesFields).filter(function(el) { return el.name === 'attribute'; }).map(function(el) { return el.value; });
        if (attributes.length === 0) attributes = null;
        if (typeof attributes === 'string') attributes = [attributes];
        if(!attributes) {
            document.getElementById(validateField).value = 'no-attributes';
        } else {
            document.getElementById(validateField).value = 'has-attributes';
        }

        if (productForm.validator.validate()) {
            var obj = {
                attributes: encode_base64(attributes.join(',')).replace(new RegExp('/','g'),'%2F').replace(new RegExp('=','g'),'%3D')
            };
            var url = urlTemplate.replace(productTemplateSyntax, function(m, p1, p2, p3) { return p1 + (obj[p3] !== undefined ? obj[p3] : ''); });
            setLocation(url);
        }
    }

    function checkMaxLength(Object, MaxLen)
    {
        if (Object.value.length > MaxLen-1) {
            Object.value = Object.value.substr(0, MaxLen);
        }
        return 1;
    }

    function openDuplicateDialog(keepImagesUrl, skipImagesUrl) {
        var html = '<p><?php echo $this->__('This product has images. Do you want to create a duplicate product that includes them?'); ?></p>';

        function duplicateKeepImages(dialogWindow) {
            dialogWindow.close();
            setLocation(keepImagesUrl);
        }
        function duplicateSkipImages(dialogWindow) {
            dialogWindow.close();
            setLocation(skipImagesUrl);
        }

        Dialog.confirm(html, {
            width: 505,
            height: "auto",
            draggable: true,
            closable: true,
            className: "magento",
            windowClassName: "popup-window",
            title: '<?php echo $this->__('Duplicate Product') ?>',
            recenterAuto: false,
            hideEffect: function(el){el.style.display='none'},
            showEffect: function(el){el.style.display=''},
            id: "duplicate-product",
            buttonClass: "form-button",
            okLabel: "<?php echo $this->__('Duplicate with images'); ?>",
            ok: duplicateKeepImages.bind(this),
            cancelLabel: "<?php echo $this->__('Duplicate without images'); ?>",
            cancel: duplicateSkipImages.bind(this),

            onShow: function(dialogWindow) {
                var content = document.getElementById('duplicate-product_content');
                if (content) {
                    content.style.paddingTop = '1.25em';
                }
                if (dialogWindow && dialogWindow.element) {
                    var buttons = dialogWindow.element.querySelectorAll('.magento_buttons');
                    buttons.forEach(function(button) {
                        button.style.textAlign = 'center';
                    });
                }
            }
        });
    }

window.addEventListener('load', function() {
    var objName = '<?php echo $this->getSelectedTabId() ?>';
    if (objName) {
        obj = document.getElementById(objName);
        //IE fix (bubbling event model)
        product_info_tabsJsTabs.setSkipDisplayFirstTab();
        product_info_tabsJsTabs.showTabContent(obj);
    }
    Product.AttributesBridge.setTabsObject(product_info_tabsJsTabs);
});

Validation.addAllThese([
    ['validate-super-product-attributes', '<?php echo Mage::helper('core')->jsQuoteEscape(Mage::helper('catalog')->__('Please select one or more attributes.')) ?>', function(v) {
                return (v != "no-attributes");
            }]]);
//]]>
</script>
